import 'package:flutter/material.dart';

class LimitPageCustomFormWidget extends StatelessWidget {
  const LimitPageCustomFormWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
